# 執筆・追加ルール

このガイドを増やす／直すときの決まりごと。読者は **R を使う統計プログラマ**。
「R でおなじみのやり方を出発点に、Python での最善手へ橋渡しする」ことだけを目的にする。

## 1 項目の構成（固定フォーマット）

`TEMPLATE.md` をコピーして `docs/topics/topic-NNN.md` を作る。各項目は次の見出しを持つ。

1. `# NNN. タイトル — Rの代表関数 → Pythonの代表手段`
2. `!!! abstract` … この項目の R→Python 対応を 3 行で要約
3. `## R ではこう書く` … **メジャーな方法を複数**挙げ、**使い分け**を明示する
4. `## Python ではこう書く` … `=== "pandas"` / `=== "polars"` タブ。**推奨を 1 つ明言**する
5. `## 対応早見表` … R / pandas / polars の 3 列表
6. `## つまずきポイント` … R と Python の差でハマる所（`!!! warning`）
7. `## 関連項目` … 他項目への相対リンク

## 番号（安定 ID）の扱い

- 番号は **一度振ったら動かさない**。新項目は**末尾の空き番号**に足す。
- 表示順は `mkdocs.yml` の `nav:` だけで決める。カテゴリを組み替えても番号はそのまま。

## コード検証（必須）

数値・出力を書いたら、必ず実行して確認する。

```bash
# R 側
Rscript -e '...'

# Python 側
python -c '...'
```

- コードブロックの直後に、`出力:` として**実際の出力**を貼る。
- 環境差が出る所（浮動小数の桁、`options(digits=)` など）は本文で断る。
- R と Python で**結果が食い違う既知の落とし穴**（丸め方式、0/1 始まり、`NA` の伝播、因子順序など）は
  `## つまずきポイント` に必ず書く。

## 文体

- 日本語。断定しすぎず、しかし「実務ではこれ」という推奨は明確に。
- 専門用語（TFL, ADaM, デモグラ表など）はそのまま使ってよい。
- R の関数は `paste0()`、Python は `str.cat()` のようにコードスパンで書く。
